
using UnityEngine;

namespace MyFramework.EditorCode
{
    public class LocatizationTextEditor : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}